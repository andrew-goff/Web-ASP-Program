@model Logistics.Models.Store

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Store</legend>

        @Html.HiddenFor(model => model.StoreId)

        <div class="editor-label">
            @Html.LabelFor(model => model.StoreOwner)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StoreOwner)
            @Html.ValidationMessageFor(model => model.StoreOwner)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.StoreAddress)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StoreAddress)
            @Html.ValidationMessageFor(model => model.StoreAddress)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
